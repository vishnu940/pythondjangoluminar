#multi threading

#multi tasking

   #  a) multi processing(executing multiple process at same time)
   #  b) multi threading(executes tasks simultaneously,it executes in the same program)

import threading
print(threading.currentThread().getName())